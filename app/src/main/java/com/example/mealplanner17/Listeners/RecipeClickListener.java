package com.example.mealplanner17.Listeners;
/**
 * The RecipeClickListener interface provides a callback method to handle the click events
 * on recipe items within a user interface.
 */
public interface RecipeClickListener {
    void onRecipeClick(String id);
}
